<!-- res/drawable/rounded_corner_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 圆角半径 -->
    <corners android:radius="10dp" />
    <!-- 填充颜色 -->
    <solid android:color="#FFFFFF" />
    <!-- 边框宽度与颜色 -->
    <stroke
        android:width="2dp"
        android:color="#000000" />
</shape>
